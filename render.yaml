services:
  - type: web
    name: land-usage-dash-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn land_usage_dash_app:server --bind 0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
